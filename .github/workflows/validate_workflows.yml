name: Validate Workflows

on:
  push:
    branches: 
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Validate main.yml
        run: yamllint .github/workflows/main.yml
